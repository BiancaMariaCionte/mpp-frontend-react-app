import { styled } from "@mui/system";  

const MyComponent = styled('div')({ //It returns a new component that renders as a <div> but with the specified styles
    color: 'darkgrey', // Change color to dark grey
    padding: 8,
    fontFamily: 'Arial, sans-serif', // Change font family
    fontSize: '30px', // Increase font size
  });



export default MyComponent